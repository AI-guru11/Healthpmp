# دليل نشر التطبيق على Netlify

## ✅ التطبيق جاهز للنشر على Netlify

تم تجهيز المشروع بالكامل للنشر على Netlify. جميع الإعدادات اللازمة تم إضافتها.

---

## 📋 الطريقة الأولى: النشر عبر Git (موصى به)

### الخطوة 1: رفع الكود إلى GitHub

```bash
# إذا لم يكن المشروع على GitHub بعد
git add .
git commit -m "Prepare for Netlify deployment"
git push origin main
```

### الخطوة 2: ربط المشروع مع Netlify

1. اذهب إلى [netlify.com](https://netlify.com) وسجل دخول
2. اضغط على **"Add new site"** → **"Import an existing project"**
3. اختر **GitHub** (أو GitLab/Bitbucket)
4. اختر المستودع `health-pwa`
5. الإعدادات ستظهر تلقائياً من ملف `netlify.toml`:
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
   - **Node version**: 20
6. اضغط **"Deploy site"**

### الخطوة 3: إضافة المتغيرات البيئية (Environment Variables)

1. في لوحة تحكم Netlify، اذهب إلى:
   **Site settings** → **Environment variables** → **Add a variable**

2. أضف المتغيرات التالية:

```
Key: VITE_GEMINI_API_KEY
Value: [ضع مفتاح Gemini API الخاص بك]

Key: VITE_FIREBASE_CONFIG
Value: [ضع إعدادات Firebase بصيغة JSON]
```

3. احفظ واعد بناء الموقع: **Deploys** → **Trigger deploy**

---

## 📋 الطريقة الثانية: النشر المباشر (عبر CLI)

### الخطوة 1: تثبيت Netlify CLI

```bash
npm install -g netlify-cli
```

### الخطوة 2: تسجيل الدخول

```bash
netlify login
```

### الخطوة 3: بناء المشروع

```bash
cd /home/ubuntu/health-pwa
npm run build
```

### الخطوة 4: النشر

```bash
# نشر تجريبي
netlify deploy

# نشر نهائي للإنتاج
netlify deploy --prod
```

اتبع التعليمات:
- اختر **"Create & configure a new site"**
- اسم الموقع: اختر اسم فريد (مثال: `my-health-coach`)
- Publish directory: اكتب `dist`

---

## 🔒 إضافة المتغيرات البيئية عبر CLI

```bash
netlify env:set VITE_GEMINI_API_KEY "your_api_key_here"
netlify env:set VITE_FIREBASE_CONFIG '{"apiKey":"...","projectId":"..."}'
```

---

## 📋 الطريقة الثالثة: السحب والإفلات (Drag & Drop)

### الأسهل للمبتدئين:

1. قم ببناء المشروع محلياً:
```bash
npm run build
```

2. اذهب إلى [app.netlify.com/drop](https://app.netlify.com/drop)

3. اسحب مجلد `dist` كاملاً وأفلته في المتصفح

4. الموقع سيُنشر فوراً! 🎉

⚠️ **ملاحظة**: هذه الطريقة لا تدعم التحديثات التلقائية. يجب إعادة رفع `dist` يدوياً عند كل تحديث.

---

## 🌐 بعد النشر

### رابط التطبيق

بعد النشر، ستحصل على رابط مثل:
```
https://your-site-name.netlify.app
```

### تخصيص النطاق (اختياري)

1. في لوحة التحكم: **Domain settings** → **Add custom domain**
2. أدخل نطاقك الخاص (مثال: `myapp.com`)
3. اتبع التعليمات لتحديث DNS

### شهادة SSL (HTTPS)

Netlify توفر SSL مجاناً وبشكل تلقائي! ✅

---

## 🔍 التحقق من نجاح النشر

### 1. اختبر التطبيق

افتح الرابط في المتصفح وتأكد من:
- ✅ واجهة المستخدم تظهر بشكل صحيح
- ✅ الاتجاه RTL يعمل (العربي من اليمين لليسار)
- ✅ يمكن إنشاء ملف مستخدم جديد
- ✅ البيانات تُحفظ في IndexedDB

### 2. اختبر PWA

في Chrome على الهاتف:
- افتح التطبيق
- من القائمة: **Add to Home Screen**
- تأكد من ظهور أيقونة التطبيق على الشاشة الرئيسية

### 3. اختبر الوضع غير المتصل (Offline)

- افتح التطبيق
- أغلق الإنترنت
- التطبيق يجب أن يعمل بدون مشاكل! ✅

---

## 🎯 مميزات النشر على Netlify

- ✅ **مجاني بالكامل** للمشاريع الصغيرة
- ✅ **HTTPS تلقائي** (SSL مجاني)
- ✅ **CDN عالمي** (سرعة عالية في جميع الدول)
- ✅ **تحديثات تلقائية** من Git
- ✅ **معاينة لكل Pull Request**
- ✅ **دعم PWA كامل**
- ✅ **Analytics مدمج** (اختياري)

---

## ⚙️ ملفات التكوين المضافة

### `netlify.toml`
ملف تكوين Netlify الذي يحتوي على:
- أمر البناء (`npm run build`)
- مجلد النشر (`dist`)
- إعادة توجيه لدعم SPA
- Headers أمنية (XSS Protection, Frame Options, إلخ)
- قواعد التخزين المؤقت للملفات الثابتة
- تكوين خاص لـ Service Worker

### `vite.config.ts`
تم تحديث:
- `base: '/'` (بدلاً من `/health-pwa/`)
- `start_url: '/'` في الـ manifest

---

## 🔧 تحديث التطبيق بعد النشر

### إذا استخدمت Git:

```bash
# قم بتعديل الكود
git add .
git commit -m "Update feature X"
git push origin main
```

Netlify سيبني وينشر التحديث تلقائياً! 🚀

### إذا استخدمت CLI:

```bash
npm run build
netlify deploy --prod
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة: التطبيق لا يعمل بعد النشر

**الحل:**
```bash
# تأكد من بناء التطبيق بدون أخطاء
npm run build

# تحقق من السجلات في Netlify
# Deploys → [اختر آخر Deploy] → Deploy log
```

### المشكلة: Service Worker لا يعمل

**الحل:**
- تأكد من فتح الموقع عبر HTTPS (ليس HTTP)
- افتح Developer Tools → Application → Service Workers
- تأكد من وجود Service Worker مسجل

### المشكلة: متغيرات البيئة لا تعمل

**الحل:**
- تأكد من إضافة المتغيرات في Netlify Dashboard
- تأكد من أن أسماء المتغيرات تبدأ بـ `VITE_`
- أعد بناء الموقع بعد إضافة المتغيرات

### المشكلة: الصور/الأيقونات لا تظهر

**الحل:**
- تأكد من وجود الملفات في `public/icons/`
- أضف الصور:
```bash
# أضف أيقونات التطبيق (192x192 و 512x512)
cp your-icon-192.png public/icons/icon-192.png
cp your-icon-512.png public/icons/icon-512.png
```

---

## 📊 مراقبة الأداء

### Netlify Analytics (مدفوع)

للحصول على إحصائيات مفصلة:
- عدد الزوار
- الصفحات الأكثر زيارة
- مصادر الزيارات

### بديل مجاني: Google Analytics

يمكنك إضافة Google Analytics في `index.html`:

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

---

## 🎉 تم الإعداد بنجاح!

التطبيق الآن جاهز للنشر على Netlify. اختر الطريقة الأنسب لك وابدأ! 🚀

**روابط مفيدة:**
- [Netlify Docs](https://docs.netlify.com/)
- [Netlify CLI Docs](https://cli.netlify.com/)
- [PWA on Netlify](https://www.netlify.com/blog/2019/05/30/how-to-deploy-a-pwa-with-netlify/)
